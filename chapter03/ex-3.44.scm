;; Louis is not right. The essential difference between the transfer problem
;; and the exchange problem is that the difference of the two account balances
;; involved in an exchange may be changed by other concurrent transactions if
;; the exchange procedure is not serialized, while the transfer amount is given
;; and will not change in the transfer problem.
